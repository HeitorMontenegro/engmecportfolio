import { styled } from "@mui/material/styles";
import Box from "@mui/material/Box";

const Pagina = styled(Box)(() => ({
  height: "100vh",
  width: "100vw",
  display: "flex",
}));

export default Pagina;
